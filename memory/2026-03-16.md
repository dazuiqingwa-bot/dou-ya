## 2026-03-16

- 老板确认当前正式使用的本地 Obsidian 路径更新为：`/Users/dazuiqingwa/Library/Mobile Documents/com~apple~CloudDocs/笔记`；旧记录 `/Users/dazuiqingwa/笔记` 已被替代。
- 当前 Obsidian 顶层库结构包含：`高飞库`、`苗苗库`、`禾禾库`、`家庭共享库`。
- OpenClaw 当前已同时接入两类模型认证：
  - `openai-codex:default`（OAuth / 令牌登录）
  - `anthropic:default`（Claude API Key）
- 老板要求默认模型改为 `openai-codex/gpt-5.4`，仅在特殊需求时切换到 Claude；已更新 `~/.openclaw/openclaw.json`。
- Browser Relay 在 Mac mini 迁移后出现异常，现场确认：gateway/relay/token 都可用，但这台机器最初未完成本机 Chrome 扩展安装；已执行 `openclaw browser extension install`，扩展目录为：`/Users/dazuiqingwa/.openclaw/browser/chrome-extension`。
- 如果在 Chrome 的“Load unpacked”选择器中找不到扩展目录，需要显示隐藏文件（`Cmd+Shift+.`）或用“前往文件夹”（`Cmd+Shift+G`）直达 `~/.openclaw/browser/chrome-extension`。
- 已补记两条小红书发布任务：
  1. 今天发的内容主轴是“OpenClaw 真正可怕的地方，不是更聪明，而是让‘想到’和‘做到’之间的距离突然缩短了”，采用版本 B；封面方向为“想到，就更容易做到 / OpenClaw 真正可怕的地方，不只是聪明”。
  2. 明天发的内容主轴是“小龙虾可以当员工，但你会当老板吗？”，核心观点是 AI 可以成为高强度员工，但真正关键的是目标清晰、抓重点、跑偏能拉回；AI 家庭中枢不只是多一个员工，而是让一家人都继续成长。
- Mac mini 上 OpenClaw 的受控 Chrome 与老板日常手动打开的 Chrome 不是同一个浏览器会话；即使都叫 Chrome，也可能因 profile / 调试端口 / 用户数据目录不同而互不共享登录态与扩展。要让 ASA 直接操控网页，必须在 OpenClaw 受控的 Chrome（profile: `openclaw`）里单独登录并安装所需扩展。
- 已通过 `openclaw browser` 原生命令在 Mac mini 上实际接管小红书创作服务平台，进入“发布图文笔记” → “文字配图” → 模板选择 → 正式编辑页，证明原生浏览器自动化链路可用。
- 重要操作教训：向小红书编辑器自动填充正文时，直接用命令行 `type` 发送带转义的 `\n`，会把换行当作字面字符或导致排版异常；以后应优先按编辑器实际 DOM/输入组件类型处理换行，避免出现“\n”字样或整段被压扁的发布事故。
