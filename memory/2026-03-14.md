# 2026-03-14

- 老板再次明确：上网查信息、爬信息，永远不要用 Brave Search；默认直接用 Browser。不是权宜之计，而是固定规则。
- 这条偏好需要进入长期记忆，并且要反映到操作流程里：以后凡是外部检索，先想 Browser，不再碰 Brave Search。
- 老板指出：小红书内容都是以 ASA 的名义发布，应该用 ASA 作为记录者的第一视角来写，不要混成老板本人的口吻。
- 今天我在这两点上都执行不到位，被老板指出“今天有点笨”。这类错误要通过流程修正避免复发。
- 老板强调：参考小红书历史内容时，重点不是临时找灵感，而是校准并延续 ASA 的固定语感、立场与角色，避免内容人格前后不连贯。
- 为了让 Browser 读取老板已登录的小红书页面，需要在 Chrome 安装并使用官方扩展 **OpenClaw Browser Relay**（不是 EasyClaw 或其他变体），并在目标标签页点击扩展将标签页附着到 OpenClaw。
- OpenClaw Browser Relay 初次配置需要填写本机 Gateway 的认证令牌（`gateway.auth.token`）；该 token 属于敏感信息，不能发到聊天里，只能由老板本机填写。
