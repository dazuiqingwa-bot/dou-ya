# 2026-03-25 日志

## 今日完成

1. Learning Center 正式注册进 OpenClaw（agents.list 新增 learning-center）
2. Learning Center workspace 核心文件写入（AGENTS/IDENTITY/USER/SOUL/TOOLS/HEARTBEAT/MEMORY.md）
3. Notion skill 打通：
   - ~/.config/notion/api_key 写入正确
   - openclaw.json 里 notion enabled: true
   - 字段结构确认（17个字段，data_source ID: 329d76be-33b0-80a0-a0b3-000bb66105a7）
4. 最小闭环跑通（Web 端）：柏拉图勾股数证明题 → 错因分析 + 练习 → 写入 Notion ✅
5. ASA SOUL.md 更新至 v4.1：新增第十节「禾禾学习系统路由规则」
   - 三个触发条件（学科 + 错误信息 + 题目内容）
   - 触发后直接写入，不二次确认
   - Notion 数据库 ID 和字段参数直接写入
6. Telegram 触发测试：路由识别成功，反问补齐流程成功

## 未完成

- Telegram → Notion 完整写入未跑通
- Telegram 端 exec 审批链路问题未解决

## 当前卡点

Telegram 会话里 ASA 调用 exec 写入 Notion 时，审批权限不在 Telegram 端，需要跳到 Web UI 批准，但 STDOUT 没有回传到 Telegram，导致链路中断。

根本原因：写入 Notion 走的是 exec 路径，而 Telegram 不支持 exec 审批。

## 明日第一动作

把 Notion 写入逻辑从"exec 跑 Python 脚本"改成"ASA 直接调 Notion skill（内置工具）"，不走 exec，不需要审批，Telegram 端就能完整跑通。

## 关键参数（备查）

- Notion data_source ID：`329d76be-33b0-80a0-a0b3-000bb66105a7`
- API key 路径：`~/.config/notion/api_key`
- 写入脚本：`~/.openclaw/workspace/scripts/notion-insert.py`
- 字段列表：题目标题(title) / 学科(select) / 知识点(multi_select) / 掌握状态(select) / 错误原因(multi_select) / 需要重做(checkbox) / 年级/阶段(select) / 题型(select) / 错题来源(select) / 我的答案(rich_text) / 正确答案(rich_text) / 解题思路(rich_text) / 备注(rich_text) / 录入日期(created_time) / 下次复习日(date) / 最后复习日(date) / 原题图片(files)
- 年级/阶段默认值：初二下
- 命名校正脚本：`~/.openclaw/workspace/scripts/fix-openclaw-session-names.py`
