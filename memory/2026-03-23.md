# 2026-03-23

- 老板要求将本机 OpenClaw 从 2026.3.13 升级到 2026.3.22；升级已完成，当前版本确认已为 `OpenClaw 2026.3.22`。
- 升级过程中发现 `~/.openclaw/openclaw.json` 里残留旧的 Feishu 插件路径：`/opt/homebrew/lib/node_modules/openclaw/extensions/feishu`，升级后该路径失效并导致配置校验报错；已从配置中移除该 `plugins.load.paths` 条目。
- 为了从 webchat 代老板执行升级与修复，曾临时开启 `tools.elevated.allowFrom.webchat = ["*"]`；随后根据老板选择已改回方案 A，将 webchat 的 elevated wildcard 收回，目前配置为 `tools.elevated.allowFrom = {}`。
- 老板明确表达了新的长期偏好：希望 **webchat** 和 **Telegram** 都支持“审批式提权”工作流，即 ASA 发起需要提权的操作后，由老板在界面中选择 `allow-once / allow-always / deny`，而不是让老板自己去终端执行命令。
- 本次安全检查结果：在收回 webchat elevated wildcard 后，系统状态恢复为 `0 critical / 4 warn / 1 info`；剩余主要风险包括：
  - `channels.feishu.dmPolicy = "open"` 且 `channels.feishu.allowFrom = ["*"]`，导致系统仍被识别为可能多用户暴露环境；
  - `gateway.nodes.denyCommands` 中若干命令名无效，存在“看似限制但实际未生效”的假安全感。
- 老板选择继续做安全收口，并先处理 Feishu；已建议将 Feishu 的 DM 策略从 `open` 改为 `pairing`，但截至本次 flush 尚未确认完成该改动。
- 已查到关于 Telegram allowFrom 的关键规则：`channels.telegram.allowFrom` 使用的是 **Telegram 用户数字 ID**，不是 bot 用户名；后续若要为 Telegram 开启“审批式提权”白名单，需要先获取老板本人的 Telegram numeric sender ID。
